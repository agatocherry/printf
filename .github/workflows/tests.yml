name: tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2
      # - name: Install
      #   run: |
      #     brew install make

      - name: Testing
        run: cd tests && bash tests.sh all